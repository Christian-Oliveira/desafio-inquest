language: python
python:
  - "3.8"

branches:
  only:
    - master

# Start services.
services:
  - db

# command to install dependencies.
install: "pip install -r requirements.txt"

before_script:
  - cp .env.example .env
  - python manage.py migrate
sudo: false